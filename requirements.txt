# Pinned to the versions this project was built and tested against.
# langchain 1.x is recent and has broken across majors before, so an
# unpinned install will not reproduce this environment.
python-dotenv==1.2.2
pypdf==6.14.2
langchain-core==1.5.3
langchain-text-splitters==1.1.2
langchain-openai==1.4.1
langchain-chroma==1.1.0
chromadb==1.5.9
streamlit==1.61.1

# Linux only, and only for deployment: chromadb needs sqlite >= 3.35 and some
# hosting images ship an older one. There are no Windows/macOS wheels, hence
# the marker - a local install on either simply skips it. app.py swaps it in
# for the stdlib sqlite3 when present.
pysqlite3-binary==0.5.4.post2; sys_platform == "linux"
